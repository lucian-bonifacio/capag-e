**Input** — text field, most often used as a search box above account lists and audit tables.

```jsx
<Input iconLeft={<SearchIcon/>} placeholder="Buscar conta ou grupo…" />
```

Pass `iconLeft` for a leading icon. Sizes `sm` / `md`. Focus shows a blue ring. Use for search and simple text entry — not for large forms.
